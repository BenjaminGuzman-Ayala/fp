
  <div class="accordion-item">
    <h2 class="accordion-header">
      <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#school<%= school.id %>" aria-expanded="false" aria-controls="school<%=school.id%>">
        <%= school.name %>
      </button>
    </h2>
    <div id="school<%= school.id %>" class="accordion-collapse collapse" data-bs-parent="#accordionFlushExample">
      <div class="accordion-body">  <p><%=school.address  %></p>    
    <button class="btn btn-secondary">
    <%= link_to "Show", school, style: "color:white; text-decoration: none;" %>
    </button>
      

      </div>
    </div>
  </div>
